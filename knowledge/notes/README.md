# Notes — evergreen project knowledge

This folder holds durable, reusable project knowledge accrued by `/wrap` — one topic per file,
cross-linked with `[[wikilinks]]`. It is *what we know*, distinct from `docs/` (*how the app works*)
and `logs/` (*what happened* this session).

# Session logs

Committed build-session logs — one file per day, `YYYY-MM-DD.md`, appended by `/wrap`. The journal
is **tracked** (not gitignored) on purpose: in this repo the workflow is part of the deliverable, so
the agentic process stays visible.

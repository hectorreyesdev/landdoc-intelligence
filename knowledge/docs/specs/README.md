# Specs

One feature spec per file, named `NNNN-<slug>.md`. Open a new one with `/spec`; link them here as
they're written. A spec is also where any change to the `IChatClient` / `IEmbeddingClient` ports
must be designed before code (per the `CLAUDE.md` guardrail).

- [0001 — Document Ingestion (Write Path)](0001-document-ingestion-write-path.md) — **Accepted**

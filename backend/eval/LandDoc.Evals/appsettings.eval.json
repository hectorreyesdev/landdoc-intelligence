{
  "//": "Eval runner config (spec 0012, ADR-0021). Provider selection for the FULL PROD stack + eval knobs. NO SECRETS — Search/AzureOpenAI/Anthropic credentials come from user-secrets / env / Key Vault. The fixture appends a unique Search:IndexName (landdoc-eval-{runId}) at run time so the live landdoc-chunks index is never touched.",
  "ModelClient": {
    "ChatProvider": "azureopenai",
    "EmbeddingProvider": "azureopenai"
  },
  "VectorStore": {
    "Provider": "azuresearch"
  },
  "DocumentStore": {
    "Provider": "azureblob"
  },
  "Search": {
    "IndexName": "landdoc-eval"
  },
  "Eval": {
    "JudgeModel": "claude-sonnet-4-6",
    "Thresholds": {
      "//": "Report-only by default (Enabled=false). Set Enabled=true to turn the per-metric floors below into hard pass/fail gates. Keys must match the metric names: Recall@K (0..1), Groundedness (1..5), Equivalence (1..5).",
      "Enabled": false,
      "Recall@K": 0.8,
      "Groundedness": 4.0,
      "Equivalence": 4.0
    }
  }
}
